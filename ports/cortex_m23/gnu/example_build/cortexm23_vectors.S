/**************************************************************************
 * Copyright (c) 2026 Eclipse ThreadX contributors
 *
 * This program and the accompanying materials are made available under the
 * terms of the MIT License which is available at
 * https://opensource.org/licenses/MIT.
 *
 * AI Disclosure: This file was largely AI-generated by Claude Code (Opus 5).
 * The AI-generated portions may be considered public domain (CC0-1.0)
 * and not subject to the project's licence. The human contributor has
 * reviewed and verified that the code is correct.
 *
 * SPDX-License-Identifier: MIT and CC0-1.0
 **************************************************************************/

/*  Exception and interrupt vector table for the Cortex-M23 example.
 *
 *  Armv8-M names its exception handlers the CMSIS way, so this table cannot be
 *  the Armv7-M one with the core name changed: the port and its low level
 *  initialisation export SVC_Handler, PendSV_Handler, SysTick_Handler and
 *  HardFault_Handler, where the Cortex-M3, M4 and M7 examples reference
 *  __tx_SVCallHandler, __tx_PendSVHandler, __tx_SysTickHandler and
 *  __tx_HardfaultHandler. Linking the Armv7-M table here fails to resolve every
 *  one of those.
 *
 *  Slot 7 is SecureFault on Armv8-M, where Armv7-M leaves it reserved.
 *
 *  Cortex-M23 is Armv8-M Baseline, which has no configurable faults, so the
 *  MemManage, BusFault and UsageFault slots go to __tx_BadHandler. This port
 *  also exports no __tx_NMIHandler or __tx_DBGHandler, unlike the Mainline
 *  ports, so those two slots take __tx_BadHandler as well.
 *
 *  The SVCall slot is __tx_BadHandler rather than SVC_Handler because this port
 *  is built with -DTX_SINGLE_MODE_NON_SECURE, and tx_thread_schedule.S compiles
 *  SVC_Handler only when neither TX_SINGLE_MODE_SECURE nor
 *  TX_SINGLE_MODE_NON_SECURE is defined. Naming it here does not fail to link
 *  loudly, it fails to link at all.
 */

  .global reset_handler
  .global __tx_BadHandler
  .global PendSV_Handler
  .global SysTick_Handler
  .global HardFault_Handler

  .syntax unified
  .section .vectors, "ax"
  .code 16
  .align 0
  .global _vectors

_vectors:
  .word __stack_end__
  .word reset_handler
  .word __tx_BadHandler
  .word HardFault_Handler
  .word __tx_BadHandler       // MemManage
  .word __tx_BadHandler       // BusFault
  .word __tx_BadHandler
  .word __tx_BadHandler       // SecureFault, Armv8-M only
  .word 0 // Reserved
  .word 0 // Reserved
  .word 0 // Reserved
  .word __tx_BadHandler       // SVCall: see the note above
  .word __tx_BadHandler
  .word 0 // Reserved
  .word PendSV_Handler        // Used by the ThreadX scheduler
  .word SysTick_Handler       // Used by the ThreadX timer
  .word __tx_BadHandler       // Populate with a user interrupt handler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler
  .word __tx_BadHandler

  .section .init, "ax"
  .thumb_func
reset_handler:
  // low level hardware config, such as PLL setup goes here
  b _start
