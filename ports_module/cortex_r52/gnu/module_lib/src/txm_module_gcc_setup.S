@/***************************************************************************
@ * Copyright (c) 2026 Eclipse ThreadX contributors
@ *
@ * This program and the accompanying materials are made available under the
@ * terms of the MIT License which is available at
@ * https://opensource.org/licenses/MIT.
@ *
@ * AI Disclosure: This file was largely AI-generated by Claude Code (Opus 5).
@ * The AI-generated portions may be considered public domain (CC0-1.0)
@ * and not subject to the project's licence. The human contributor has
@ * reviewed and verified that the code is correct.
@ *
@ * SPDX-License-Identifier: MIT and CC0-1.0
@ **************************************************************************/
@
@/**************************************************************************/
@/*                                                                        */
@/*  MODULE LIBRARY                                          RELEASE       */
@/*                                                                        */
@/*    txm_module_gcc_setup.S                           Cortex-R52/GNU     */
@/*                                                           6.5.2        */
@/*  AUTHOR                                                                */
@/*                                                                        */
@/*    Frédéric Desbiens, Eclipse Foundation                               */
@/*                                                                        */
@/*  DESCRIPTION                                                           */
@/*                                                                        */
@/*    Where a position-independent module fixes up its own data            */
@/*    references, and currently a no-op.                                   */
@/*                                                                        */
@/*    The module shell entry calls this before running the module, passing  */
@/*    the address the module's code was loaded at.  On the Armv8-M module   */
@/*    port the equivalent copies the global offset table and rewrites every */
@/*    entry so the module's data references point at the memory the manager */
@/*    allocated, rather than wherever the module was linked.               */
@/*                                                                        */
@/*    This port does not need that yet, because the module is linked        */
@/*    absolutely at the address it runs from and loaded in place there.     */
@/*    Nothing needs relocating, so there is nothing to do.                  */
@/*                                                                        */
@/*    That is a stepping stone and not the destination.  Absolute linking   */
@/*    means one instance of a module, at one fixed address, decided at      */
@/*    build time -- which is not how modules are used.  Making them         */
@/*    genuinely relocatable means building position independent, having the */
@/*    manager process the relocations, and implementing this function for   */
@/*    real.  The hook is here so that work has somewhere to land and so     */
@/*    the shell entry does not have to change when it does.                 */
@/*                                                                        */
@/*  INPUT                                                                 */
@/*                                                                        */
@/*    r0    Address the module's code was loaded at                        */
@/*                                                                        */
@/**************************************************************************/

    .syntax unified
    .arm
    .text
    .align  2

    .global _gcc_setup
    .type   _gcc_setup, %function

_gcc_setup:

@   Nothing to relocate while modules are linked absolutely.  r0 holds the load
@   address and is deliberately ignored rather than removed from the interface,
@   because the real implementation needs it.

    BX      lr
